# kts.blackbirdzzzz.art API

Minimal FastAPI bootstrap for the AI Architect MVP.

## Run

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```
